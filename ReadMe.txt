this folder contains all the files from the dev version of coupon-system-frontend
except the node-modules folder because of its size.

to download needed files follow:

1. extract rar file into your angular workspace.
2. open project folder in visual studio terminal and type 'npm install' and 'audit fix' if necessary.
3. type npm start to run site on 'http://localhost:4200'